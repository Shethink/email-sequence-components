The only purpose of this component is to give a default to `xl` (instead of default `lg`) to the MaterialUI Container.

## Variants

### Default

```jsx
<Container>Some text</Container>
```

### Full Width

```jsx
<Container fullWidth>Some text</Container>
```
